#include <stdio.h>

int main() {
    int p[]={1,2,3,4,1}, f[3]={-1,-1,-1}, i,j,k=0;
    for(i=0;i<5;i++){
        for(j=0;j<3&&f[j]!=p[i];j++);
        if(j==3){ f[k]=p[i]; k=(k+1)%3; }
        printf("%d: %d %d %d\n", p[i], f[0], f[1], f[2]);
    }
    return 0;
}
