#include <stdio.h>
#define MAX 5

int q[MAX], f = 0, r = -1, c = 0;

void produce(int x){ if(c<MAX){ q[++r]=x; c++; } }
void consume(){ if(c>0){ printf("Consumed %d\n", q[f++]); c--; } }

int main() {
    produce(10);
    produce(20);
    consume();
    produce(30);
    consume();
    return 0;
}
