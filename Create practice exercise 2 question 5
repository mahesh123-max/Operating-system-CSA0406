#include <stdio.h>
#include <unistd.h>

int main() {
    for (int i = 0; i < 3; i++) {
        if (fork() == 0) {
            printf("Child %d: PID=%d PPID=%d\n", i + 1, getpid(), getppid());
            return 0;
        }
    }
    sleep(1);
    return 0;
}
